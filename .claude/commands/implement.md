---
description: Executa uma task de uma SPEC no papel implementor
argument-hint: SPEC-XXX T-nn
---

Leia `prompts/workflows/implement.md` e siga por completo.

**Execute pelo subagente `implementor`.** No Claude Code o papel é um subagente com
modelo e ferramentas próprios — não execute inline na sessão principal.

**{ARGS}** = $ARGUMENTS

Este arquivo é só um atalho. A instrução real, compartilhada por todas as ferramentas
de IA do projeto, está em `prompts/workflows/implement.md`. Não duplique regra aqui —
edite lá.